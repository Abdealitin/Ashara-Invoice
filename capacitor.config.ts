import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ashara Collection 1466',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
